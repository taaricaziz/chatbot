node_modules/
.next/
out/
.env
.env*.local
next-env.d.ts
*.tsbuildinfo

# Any env file, however it is suffixed. `.env` alone does NOT match
# `.env.backup-...` or `.env.production`, and a backup of a working .env is
# a complete set of live credentials.
.env
.env.*
!.env.example
*.backup-*
.vercel
.env*
